# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_modular_headers!

target 'PhotoApp' do
  pod 'Kingfisher'
  pod 'ImagePicker'
  pod 'DKImagePickerController'
end
